GEMINI_API_KEY=
PORT=4000
DEMO_MODE=false
TRIAGE_SERVICE_URL=http://triage-service:8001
# Token akun Fonnte (fonnte.com) — dipakai untuk kirim/cek status WhatsApp saat DEMO_MODE=false.
# Tautkan device lewat dashboard Fonnte (bukan lewat aplikasi ini), lalu set Webhook URL
# device tersebut ke POST https://<domain-publik-anda>/api/wa/webhook untuk terima balasan masuk.
FONNTE_TOKEN=